apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: insi-cert
  namespace: default
spec:
  secretName: insi-tls
  dnsNames:
  - app.insim2.local
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
